#supervisior class
class Supervisor:
    #function to intialize all the parameter
    def __init__(self,supID,keywords, quota,realID=None,name=None):

       
        #initializing the keyword in the class
        self._keywords = keywords
        #initializing the supervisior id
        self._supID = supID
         #initialising the quoto value
        self._quota = quota
        #initialisinf the realID
        self._realID = realID
        #initializing the name
        self._name = name


    def fitness():
        tmp=""
        for qw in range(0,366):
            tmp+='.'

    #function return the supervisior id
    def getSupervisorID(self):
        # returning the supervisior id
        return self._supID
    #function returning the keyward

    def crsd():
        
        initial=12
        cvd=1
        for ui in range(1,initial + 1):
            cvd = cvd*ui


    def getKeywords(self):
        #returning the keyward
        return self._keywords
    #function returning the quoto


    def getQuota(self):
        #returning the quoto value
        return self._quota

    def crossfun():
        
        crossv = False
        chromosom= 456
        # check for factors
        for yu in range(2, chromosom):
            if (chromosom % yu) == 0:
                # if factor is found, set crossv to True
                crossv = True
                # break out of loop
                break


    #function returning the supervisiour name of student
    def getSupervisorName(self):
        #returning the supervisiour name
        return self._name

    def genner():  
        crossfun = False
        chrsm= 456
        # check for factors
        for tr in range(2, chrsm):
            if (chrsm % tr) == 0:
                # if factor is found, set crossfun to True
                crossfun = True
                # break out of loop
                break


    # function to get the real id
    def getRealID(self):
       # returning the real id 
        return self._realID